import { requireApiUser } from '../../../../lib/auth/access';
import { getRuntimeEnv,ensureSchema } from '../../../../lib/database';
import { errorResponse } from '../../../../lib/http';
export const dynamic='force-dynamic';
export async function GET(){try{const a=await requireApiUser();if(a.response)return a.response;const db=getRuntimeEnv().DB;await ensureSchema(db);const {results}=await db.prepare("SELECT key,value FROM sync_state WHERE key IN ('last_sync_at','fast_sync_last_at','fast_sync_checked_at','sync_status','last_webhook_at','last_sync_started_at','fast_sync_error','fast_sync_lease')").all<{key:string;value:string}>();const s=Object.fromEntries(results.map(r=>[r.key,r.value]));const last=[s.last_sync_at,s.fast_sync_last_at,s.fast_sync_checked_at].filter(Boolean).sort().at(-1)||'';const isSyncing=((s.sync_status==='running'&&Date.now()-Date.parse(s.last_sync_started_at||'')<90000)||Date.parse(s.fast_sync_lease||'')>Date.now());const isHealthy=!s.fast_sync_error&&last&&Date.now()-Date.parse(last)<300000;return Response.json({state:isSyncing?'syncing':isHealthy?'healthy':'stale',lastSyncAt:last,lastEventAt:s.last_webhook_at||''});}catch(e){return errorResponse(e);}}
