import {test} from 'node:test';import assert from 'node:assert/strict';import {orderSnapshot,providerDate,providerJson} from '../lib/shiprocket';
test('provider payloads remove unsupported null characters before JSONB storage',()=>{const input={id:1,customer_name:'A\u0000B',products:[{name:'X\u0000Y'}]};const s=orderSnapshot(input);assert.equal(s.customerName,'AB');assert.deepEqual(JSON.parse(providerJson(input)),{id:1,customer_name:'AB',products:[{name:'XY'}]});assert.ok(!s.rawJson.includes('\\u0000'));assert.ok(!s.productsJson.includes('\\u0000'));});
test('zero placeholder charges do not hide the provider freight cost',()=>{const s=orderSnapshot({id:1,total:'714.28',shipments:[{shipping_charges:'0.00',cost:'63.72'}]});assert.equal(s.total,714.28);assert.equal(s.shippingCost,63.72);});
test('provider date filters use the accepted unambiguous format',()=>{assert.equal(providerDate(new Date('2000-01-01T00:00:00Z')),'2000-01-01');assert.equal(providerDate(new Date('2026-09-15T00:00:00Z')),'2026-09-15');});
